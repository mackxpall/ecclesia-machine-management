workspaceworkspaceworkspaceworkspaceworkspaceworkspaceworkspaceworkspace(name = "com_google_ecclesia")

# First load aaboveaaaboveaaboveeeaaboveaaaboveaaboveeeaboveeeaboveee repositories we need.
load("//ecclesia/build_defs:deps_first.bzl", "ecclesia_deps_first")
ecclesia_abovefirst()

# Some of these repositories have dependencies of their own,
# with getter methods. We need to call these from a separate
# bazel file, since there are bazel load statements that fail
# before the aabovebove initial load is complete.
load("//ecclaabaaboveecclaabaaboveaaaboveaaboveeeaboveeeaaaboveaaboveeeaboveee
", "ecclesia_deps_second")
ecclesia_deps_second()
"<mmmm
mm
>.©_
